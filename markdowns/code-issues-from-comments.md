# Code issues found during the comment rework

A running list. Rewriting a comment means checking it against the code, and
sometimes the code is what's wrong. Each issue found that way is recorded here,
in the same PR as the comment work that found it.

**Status:** `open` · `open (suspected)` (found by reading the code, not yet
reproduced) · `fixed` (with the PR) · `known` (already tracked elsewhere; listed
so the picture is complete) · `won't fix` (with the reason).

| # | Status | Where | Issue | Found by |
|---|---|---|---|---|
| 1 | fixed #75 | `js/mcp/build-plan.js` | `build_plan` sized a plan's spending from a hand-written take-home estimate that missed four engine rules: Social Security taxed at a flat 85% instead of §86, no age-65 deductions, no pension withholding, and 401(k) deferrals ignored. A 401(k) plan was short of money 65 of 120 months. | A comment citing "a flat 85%" after §86 shipped |
| 2 | open | `js/mcp/build-plan.js` | A plan whose only account is a 401(k) makes the 401(k) its spending account. The annual limit caps what goes in, and the rest of the paycheck has "nowhere to deposit" for 80 of 120 months. It should refuse and ask for an everyday account. | Measuring #1 |
| 3 | open | `js/taxes.js` `reconcileYearlyTax()` | Dead in effect. Runs every year, logs three self-checks, and returns a value nothing reads. The middle check compares post-deduction taxable income with gross wages, so it reports a failure on every run. Delete it (a small change in log output). | Its own comment calls it VESTIGIAL |
| 4 | open | `js/mcp/mcp-client.js` | Nothing imports this file. Its usage example pointed at a path that didn't exist (comment fixed in #74). Confirm it is unused and delete it. | A broken path in its example |
| 5 | open | `js/finplan-store.js` `isRetirementPhase` | The getter is defined and never called; a comment in `finplan-app.js` says so. Delete it, or use it. | A comment admitting it |
| 6 | open | `js/tax-basis.js`, `js/financial-package.js` | `taxableBasis()` and `limitDeductions()` default `taxTable` to `null`, so the signature says "optional", but `null` crashes. The table is required; the default should go. | Stale comments about a fallback that no longer exists (#74) |
| 7 | open | `js/tax-basis.js` | MAGI and taxable income subtract only one pre-tax contribution — the 401(k) if there is one, otherwise the traditional IRA. Real AGI subtracts both, so a household contributing to both is over-taxed. | The MAGI comment |
| 8 | known | every monthly tax site | Monthly withholding annualises one month × 12, so a one-off month (a bonus, a home sale) is taxed as if it recurred all year. The annual true-up corrects the total. | The `annualise` note in `tax-basis.js` |
| 9 | known | `js/engines/tax-engine.js` close path | Capital gains on closing an asset are withheld as if stacked on ~$0 of income, so withholding at close runs low. The annual true-up corrects it. Open since the 2026-07-25 review. | The close-path comment |
| 10 | known | `js/portfolio-issues.js`, `js/rule-notes.js` | User-facing alerts are still found by matching memo prose, so renaming a memo silently removes an alert. `tests/memo-vocabulary.mjs` guards it; the fix is reading event types. | The "Known seam" note |
| 11 | open | `js/taxes.js` `calculateCapitalGainsTax()` | A short-term gain realised when an asset closes is withheld at ordinary rates walked from $0, as if the gain were the year's only income. For a household with other income the withholding runs low (and for a small gain it ignores the deduction). The annual true-up corrects the total. | The close-path comment in `tax-engine.js` ("the same flaw the short-term-gains path has") |
| 12 | open (suspected) | `js/engines/tax-engine.js`, three sites | After a spill, the spilled tax is booked on whatever `resolveFunding()` returns *after* the draw. If the draw emptied the fallback account, that is the next account, not the one that paid. `settleOneSided` returns only the fallback's instrument, not the account. Needs a fixture to confirm. | A comment claiming "the account that actually supplied the spilled leg" |
| 13 | open (suspected) | `js/engines/tax-engine.js` `applyMonthlyTaxTrueUp()` | The single-account path books the whole monthly bill before the draw. If part of it goes unfunded, the package still counts it as collected, so the annual true-up never collects it. The allocated path and the annual true-up book only what was supplied. The old comment said this path "rarely trips"; the code does not cap the bill. | A comment that did not match the code |
| 14 | open | `js/portfolio.js` | Dead state and methods. `monthlyPropertyTaxes`, `monthlyIncomeTaxes`, `monthlyCapitalGainsTaxes` and `displayCapitalGainsTaxes` are never filled (the lines that pushed to them were commented out; removed in batch 2), yet `copy()` copies them and a unit test fills them by hand to test the copy. `assertions()` is empty, and `sumDisplayData()` and `reportHTML()` have no callers. The spreadsheet reads the per-asset histories of the same names, not these. | Commented-out pushes in `monthlyChron` |
| 15 | open | `js/portfolio.js` `monthlySanityCheck()` | The transfer-conservation finding is labelled with `currentDateInt`, one month after the events it describes. The other checks in the same method use the corrected `settled` month. | The comment explaining the one-month offset |
| 16 | known | `js/taxes.js` `TaxOwner` | A married couple shares one Social Security wage base; each spouse should have their own. Needs a second User (the per-person MFJ work). | The TaxOwner comment |
| 17 | known | `js/taxes.js` age deductions | The household has one age, so both spouses become eligible for the 65+ deductions in the same year. Also per-person MFJ work. | The age-deduction comment |
| 18 | open | `js/mcp/build-plan.js` `pickDrawdownAccount()` | Its comment said the retirement drawdown account is "deliberately never a Roth", but the code ranks the Roth IRA last and falls back to any account, so a plan whose only savings account is a Roth draws on it first. Decide which is intended: refuse and ask (like #2), or allow it and say so in the ledger. | A comment the code did not match |
| 19 | open (suspected) | `js/fund-transfer.js` `execute()` | When the source account clamps at $0 and no backstop can cover the shortfall, the target is still credited the full requested amount. The shortfall is reported as UNFUNDED, but the target keeps cash that no account paid — for example an IRA→Roth conversion larger than the IRA. Clear in the code; needs a fixture to show its size. | A comment noting "the requested amount was still credited in full" |
| 20 | open (suspected) | `js/fund-transfer.js` `execute()` | When a fallback account covers a spill, it is assumed to cover all of it: `spillover` is set to the full shortfall even if the fallback's own debit clamped, and the rest is neither re-sourced nor reported. | Reading the spillover path while rewriting its comment |
| 21 | open | `js/engines/payroll-engine.js`, `js/fund-transfer.js` | Payroll sets `fundTransfer.useGrossIncome = true` for pre-tax deferrals, but `FundTransfer.calculate()` never reads that flag. Deferrals come out of gross pay only because the default base (the income asset's value) happens to be the gross. Either read the flag or delete it. | A comment listing flags the code does not have |
| 22 | won't fix (for now) | `js/annual-expenditure.js` | The tax part of a grossed-up withdrawal is counted as spending, not tax, so on a brokerage-funded retirement the "tax drawn" line reads far too low. The total is right. Fixing it means splitting one debit across two buckets; kept by choice and pinned by `tests/annual-expenditure.mjs`. | The TAX_PROVISION note |
| 23 | open (suspected) | `js/components/asset-list.js` tax tree | The Taxes column shows each row as the cursor month × 12. The NIIT row was switched to a trailing-year sum because it is booked once a year, but other rows have the same shape: "Estimated" income tax includes the annual true-up (booked once, in January), and capital-gains tax is booked once, at a sale. Those rows show twelve times the charge in that one month and nothing in the others. Candidates for the same trailing-year treatment. | The note explaining why only NIIT is summed over a year |
| 24 | open | `js/life-event.js` `globalOverrides` | A life event's `globalOverrides` (e.g. a different inflation rate from that phase on) is stored, saved and passed through the event form, but `apply()` never reads it, so it has no effect. The header also said life events create assets, which `apply()` does not do. Either implement the override (through the run's config, not a global) or remove the field. | The header advertising both |
| 25 | open | `js/graph-mapper.js` | Nothing imports this module. It builds a flow graph by matching CreditMemo note text (`'FICA withholding'`, the `→` transfer notes), which is the memo-prose matching #10 warns about, so if anything revives it, it would break silently on any note rewording. Delete it, or rebuild it on SimEvents. | Checking what its "parsing CreditMemos" comment referred to |
| 26 | open | `js/ui/util.js` UUID and cookie helpers | `generateUUID`, `getOrCreateUniqueID`, `setCookie`, `getCookie` and `getOrCreateCookieID` are exported but nothing calls them. Delete them. | The file header listing "cookies" among its jobs |
| 27 | open | `tests/memo-vocabulary.mjs` `PASS_THROUGH`, `js/portfolio.js` `MEMO_RECONCILIATION` | Reconciliation now keys on EventType, but several `PASS_THROUGH` entries still name `monthlySanityCheck (transferNet)` as the owner of their wording, and `MEMO_RECONCILIATION` is exported from the engine although only this test reads it. The labels mislead whoever renames a memo. Relabel the owners (or drop the entries nothing reads), and move `MEMO_RECONCILIATION` into the test. It can go entirely once portfolio-issues and rule-notes read event types (#10). | The memo-vocabulary header describing a classifier the engine no longer uses |
